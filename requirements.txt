# Dependências da aplicação (deploy Streamlit Cloud).
# Versões fixadas para builds reproduzíveis.
# Nota: pandas/openpyxl NÃO estão aqui — só o script offline importar_sabi.py
# precisa deles (ver requirements-dev.txt).
streamlit==1.58.0
anthropic==0.105.0
PyMuPDF==1.27.2.3
python-dotenv==1.2.2
